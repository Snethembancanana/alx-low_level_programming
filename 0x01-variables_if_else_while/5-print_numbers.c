#include <stdio.h>
/**
 * main - Entry point
 * Return: Always return (0) for the program to be successful
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	putchar('\n');
	return (0);
}
