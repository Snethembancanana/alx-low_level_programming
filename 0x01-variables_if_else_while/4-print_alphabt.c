#include <stdio.h>

/**
 * main - Entry point
 * Return: Always return (0)
 */
int main(void)
{
	char c = 'a'

	c = 'a'
	c != 'q'
	c != 'e'
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
